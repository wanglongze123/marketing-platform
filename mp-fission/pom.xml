<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.mp</groupId>
    <artifactId>marketing-platform</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>
  <artifactId>mp-fission</artifactId>
  <description>玩法：裂变（V3 填充）</description>

  <!-- 依赖方向由此处强制，见《开发规范》§3.2：只声明允许方向的依赖 -->
  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>com.baomidou</groupId>
      <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-mysql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.dubbo</groupId>
      <artifactId>dubbo-spring-boot-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>com.mp</groupId>
      <artifactId>mp-api-fission</artifactId>
    </dependency>
    <dependency>
      <groupId>com.mp</groupId>
      <artifactId>mp-api-activity</artifactId>
    </dependency>
    <dependency>
      <groupId>com.mp</groupId>
      <artifactId>mp-api-reward</artifactId>
    </dependency>
    <!-- 好友召回与画像查询走 mock 社交能力（V3 PR-6）。与 benefit-order / reward 依赖
         mp-api-mock 同源：真实社交系统不在本项目范围内，但「依赖不可用时各过滤器的
         失败语义」必须能被验证，故把它建模在服务边界的另一侧 -->
    <dependency>
      <groupId>com.mp</groupId>
      <artifactId>mp-api-mock</artifactId>
    </dependency>
    <!-- V4 起只留 api，不依赖 mp-activity / mp-reward 的实现 jar。理由见 mp-benefit-order/pom.xml 同处注释 -->
  </dependencies>

  <!--
    可执行 jar。V4 起本模块可作为独立服务启动（见 FissionApplication），
    故需 repackage 产出 fat jar。单进程形态不受影响 —— 那时由 mp-gateway
    的 jar 承载全部模块，本 jar 只是不被用到。
  -->
  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
            <configuration>
              <!--
                classifier 不可省。默认 repackage 会把原 jar 替换成 fat jar，
                而 fat jar 里的类挪到了 BOOT-INF/classes/ 下 —— 依赖本模块的
                其他模块（mp-gateway 及其测试）随即 import 不到任何类。

                配上之后产出两个文件：mp-xxx.jar 仍是普通库 jar 供模块间依赖，
                mp-xxx-boot.jar 是可执行 fat jar 供容器启动。
              -->
              <classifier>boot</classifier>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
