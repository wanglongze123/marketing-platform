# 营销活动平台 分阶段方案

## 0. 本文档的定位

三份文档配套使用，职责不重叠：

| 文档 | 回答什么 |
| --- | --- |
| 《技术方案》 | **怎么设计的、为什么这么设计**。表结构、接口签名、时序、四层防线的推导 —— 终态,不随阶段修改 |
| 《环境与依赖》 | **用什么版本、怎么跑起来** |
| 本文档 | **这一阶段从终态里裁哪一块、裁的时候不能裁掉什么、做到什么程度算完** |

本文档大量引用技术方案章节号（如 `§3.4`），写代码时两份对着看：本文档说「V1 建 `play_biz_record`，全量字段」，具体字段去技术方案 §3.4 抄，不在此重复。

**评审节奏**：每阶段开工前评审本文档对应章节，通过后开工。阶段细化直接补写进本文档，不另起文件。

---

## 1. 阶段划分

| 阶段 | 一句话 | 交付形态 | 演示什么 |
| --- | --- | --- | --- |
| **V0** | 骨架 | 空壳工程 + CI | 提 PR 触发 CI，全绿 |
| **V1** | 跑通 | 单进程模块化单体，下游 mock 固定成功 | 下单 → 支付回调 → 发放 → 查得到 |
| **V2** | 扛住 | 单进程，下游 mock 可注入异常 | 注入超时自动收敛不重复发放；500VU 抢 100 不超卖 |
| **V3** | 铺开 | 5 服务分布式，多实例 | 新玩法接入后段零改动；kill 实例任务被接管 |

### 1.1 为什么这样切

**按「可验证的正确性」切，不按模块切。** 每一刀落在「能不能演示一个现象」上。按模块切需要先冻结全部接口再付集成成本，而接口在实现中大概率还会变。

**先权益、后裂变。** 权益售卖链路短、状态机简单，适合把「主单 + 操作记录 + 可靠任务」这套骨架立起来。裂变的关系状态机（四态 + 两条终止边 + `active_flag` 部分唯一 + `granting_until` 过期豁免）复杂度高，压在骨架不稳时做会返工。

**V2 在 V3 之前。** 先让系统面对「下游不可靠」，再面对「分布式」。反过来的话，压测暴露的问题会混在一起 —— 分不清是锁没生效、结果分类判错，还是 RPC 序列化问题。

**分布式化放最后。** 拆服务、多实例是把已经正确的系统分布式化，正确性不来自它们。放前面会让每次调试都要起 5 个进程。

### 1.2 第二个玩法为什么排在 V3

技术方案 §5.0 的核心论断：`FissionFollowerDone` 与 `payCallback` 占据同一架构位置 —— 都是「确权事件 → 触发公共能力层发放」。

这个论断**只有在第二个玩法接入的那一刻才被证明**。裂变接入不是「补一个功能」，而是**对公共能力层抽象的验收**。接入过程中 `reward` 接口的改动量本身就是交付物 —— 改动越小，说明抽象越对。

---

## 2. V1 形状冻结清单

### 2.1 为什么先讲这个

真正导致返工的从来不是「某功能没做」，而是「某个形状定错了」。功能是**加代码**，形状是**改所有调用方**。

V1 的功能没什么可评的（happy path 而已），**第一次评审的重点就是这份清单** —— 这是唯一一次改起来还便宜的时候。

### 2.2 八项

| # | 冻结项 | V1 只需做到 | 不冻结的代价 |
| --- | --- | --- | --- |
| 1 | **幂等键进接口 + 唯一索引** | 接口参数带 `clientReqNo` / `opNo`；`uk_idempotent` 系列索引全建 | 改所有调用方签名 + 洗历史数据 |
| 2 | **下游返回四分类枚举** | 定义 `RetStatus{SUCCESS, FAIL, PROCESSING, UNKNOWN}`（命名以技术方案 §4.1 为准）；mock 只返 `SUCCESS`，其余分支 `throw UnsupportedOperationException("V2")` | 若 V1 返 `boolean`，V2 要改每个调用点的分支逻辑 |
| 3 | **状态枚举一次定全** | 三条子状态线按 §3.4 全部写进代码，V1 用不到的也写 | 加枚举值要写 migration，且所有查询条件要重新审查 |
| 4 | **`play_op_record` 建表并写入** | 建表 + 每个写操作插一条 | 后加等于在每条写路径中间插一层，且要与事务边界重新对齐 |
| 5 | **事务边界收成一个方法** | 状态变更用一个 `@Transactional` 方法包住 | 可靠任务表要求「任务写入与状态变更同事务」，边界散了就要重构 |
| 6 | **`reward` 接口通用化** | 入参只有 `playType / activityId / bizOrderNo / opNo / receiverId / rewardItems[]`，**不出现 `orderId` / `tradeNo` 等权益售卖专有概念** | 裂变接入时要改接口，「后段零改动」的论断直接落空 |
| 7 | **Maven 模块按终态切** | 建全部 module，模块间 `injvm` 调用 | V3 拆服务变成重写而非改配置 |
| 8 | **异常映射方向** | facade 层：`BizException` → `FAIL`，**其余未预期异常 → `UNKNOWN`** | 未预期异常若映射为 `FAIL`，调用方会走补偿路径，而下游可能已执行成功 |

**八项加起来，V1 的额外工作量约半天。** 省下它们，V2 起步就是重构。

第 8 项容易被忽略：V1 阶段 mock 固定成功，几乎不会触发异常分支，很容易顺手写成 `catch (Exception e) { return FAIL; }`。这行代码在 V2 引入真实异常后就是资损入口。

### 2.3 第 6 项展开（最容易做错）

V1 只有权益售卖一个调用方，写 `reward` 接口时手会自然地把 `orderId` 塞进去 —— 因为当下只有它。等 V3 接裂变时才发现这是「权益专用发放接口」，不是「通用发放接口」。

**判据**：写完 `grantReward` 签名后，问自己「裂变的师徒双向发奖能不能原样调用这个方法」。答案必须是能。裂变发放的入参是「师傅 userId + 奖励包」，没有订单号 —— 所以 `bizOrderNo` 必须是**通用业务单号**，语义是「调用方的业务标识」，不是「订单号」。

### 2.4 评审时逐项确认的方式

第一次评审的产出应当是这张表填满，而不是「大家觉得没问题」：

| 冻结项 | 确认方式 |
| --- | --- |
| 1 幂等键进接口 | 打开 `CreateTradeReq` / `GrantRewardReq`，确认幂等字段存在；打开 DDL，确认唯一索引存在 |
| 2 四分类枚举 | 打开 `RetStatus.java`，确认四个值；打开调用点，确认是 `switch` 四分支而非 `if (success)` |
| 3 状态枚举定全 | 与技术方案 §3.4 的状态迁移表逐行对照 |
| 4 `play_op_record` | 打开 DDL 与写入代码 |
| 5 事务边界 | 全局搜索 `@Transactional`，确认每处都只包状态变更，且方法内无 RPC |
| 6 `reward` 接口通用化 | 逐字段念一遍 `GrantRewardReq`，任何一个字段说不出「裂变怎么传」即为不通过 |
| 7 模块切分 | `mvn dependency:tree` 看依赖方向 |
| 8 异常映射 | 全局搜索 `catch`，确认没有 `catch (Exception) → FAIL` |

---

# V0：工程骨架

> 预计半天。一个人做完，另两人拉下来能跑起来即可。

## 3. V0 做什么

V0 不写业务代码，只回答一个问题：**这套工程结构、构建、CI、测试基础设施，是不是真的能跑通。**

产出是一个 **walking skeleton** —— 一条极简但**贯穿全部层次**的链路，把每一样基础设施都实际用一遍。用不上的基础设施不在 V0 引入。

### 3.1 交付清单

| # | 项 | 内容 |
| --- | --- | --- |
| 1 | 仓库 | 建仓、`main` 分支保护、`.gitignore`、`README.md` |
| 2 | 构建骨架 | 根 `pom.xml` + 全部 module 空壳，版本按《环境与依赖》锁定 |
| 3 | 依赖方向 | 各 module 的 `pom.xml` 只声明允许方向的依赖 |
| 4 | 格式化 | Spotless 绑到 `verify` 阶段 |
| 5 | Git 钩子 | `git-build-hook-maven-plugin` + `.githooks/commit-msg` + `.githooks/pre-commit` |
| 6 | 中间件 | `docker-compose.yml`，V0/V1 只需 MySQL |
| 7 | Flyway | 四个库的 `CREATE DATABASE` + 迁移目录骨架；V0 只有一个脚本 `V0201__create_smoke_record.sql`（发奖库） |
| 8 | 冒烟链路 | 见 §3.2 |
| 9 | 集成测试 | Testcontainers 起真实 MySQL，跑通冒烟链路；surefire / failsafe 按 `*Test` / `*IT` 分工 |
| 10 | CI | GitHub Actions，见 §3.4 |
| 11 | 基础设施代码 | traceId 过滤器 + MDC 透传、`@RestControllerAdvice` 全局异常、`logback-spring.xml` |
| 12 | PR 模板 | `.github/PULL_REQUEST_TEMPLATE.md`，内容见《开发规范》§2.1 |

**第 11 项放在 V0 而非 V1**：traceId 与全局异常处理是横切能力，V1 一旦开始写业务代码再补，每个已写的 facade 都要回头改一遍。

### 3.2 冒烟链路的设计

冒烟链路要**贯穿全部层次**，否则验证不到分层是否真的成立。

```
HTTP GET /smoke/{bizNo}
    ↓ gateway
BenefitOrderService.smoke(bizNo)          玩法层
    ↓ Dubbo injvm
RewardService.smoke(bizNo)                公共能力层
    ↓ Dubbo injvm
MockProviderService.smoke(bizNo)          下游 mock
    ↓ 返回
reward 写一行 smoke_record（Flyway 建表）    验证 Flyway + MyBatis-Plus + 事务
    ↓ 返回
gateway 返回 {"code":0,"data":{"bizNo":"...","chain":["gateway","benefit-order","reward","mock"]}}
```

**这条链路验证了七件事**：多模块能编译、Dubbo `injvm` 能通、依赖方向没写反、Flyway 能在真实 MySQL 跑起来、MyBatis-Plus 能写库、Spring Boot 3.5 + JDK 21 组合可用、Testcontainers 能起容器。

**冒烟链路刻意走「逐层向下」**（玩法层 → 公共能力层 → 下游），是为了把四层都串一遍。但这不是唯一合法形状 —— V1 的 `createTrade` 直接调 `mock-downstream` 的支付服务、跨过公共能力层，同样合法（《开发规范》§3.2 规则 1：跨层向下允许）。

**`smoke_record` 建在 `db_reward`**（脚本 `V0201__create_smoke_record.sql`，放 `mp-reward` 模块下），因为链路末端是 reward 写库。表结构极简：`id` + `biz_no` + `create_time`。

**它和全部 `smoke` 方法在 V1 结束时删除**（`V1203__drop_smoke_record.sql`，不修改 V0 的历史脚本）。它是脚手架，不是功能。

### 3.3 依赖方向的验证方式

依赖方向靠 `pom.xml` 编译期强制（见《开发规范》§3.2）。V0 要**实际验证一次它真的会挡住**：

1. 临时在 `mp-reward/pom.xml` 里加一条对 `mp-benefit-order` 的依赖（下层依赖上层）
2. `mvn clean compile`，确认**构建失败**
3. 删掉这条依赖，确认构建恢复
4. 把这次验证的结论写进 PR 描述

不做这一步的话，「依赖方向编译期强制」只是一句话，没人知道它是否真的生效。

### 3.4 CI 必过项

GitHub Actions，触发条件：向 `main` 提 PR、push 到 `main`。

| # | 检查 | 命令 |
| --- | --- | --- |
| 1 | commit message 格式 | 正则校验 Conventional Commits |
| 2 | 代码格式 | `mvn spotless:check` |
| 3 | 编译 | `mvn clean compile` |
| 4 | 单测 | `mvn test` |
| 5 | 集成测试 | `mvn verify`（Testcontainers 起 MySQL） |
| 6 | Flyway 从零执行 | 包含在集成测试内 —— 容器是全新的库，迁移必须从零跑通 |
| 7 | 文档一致性 | `python3 docs/check-docs.py`（V1 补入，见 §4.8） |

任一不过不允许合入。CI 用 JDK 21，与本地一致。第 7 项在 V0 阶段为 6 项，V1 起为 7 项。

**CI 上不要跑 `mvn validate` 装钩子** —— CI 环境不需要 git 钩子，且 `git-build-hook-maven-plugin` 在 detached HEAD 下可能报错。CI 直接从 `spotless:check` 开始。

### 3.5 目录结构

```
marketing-platform/
├── pom.xml
├── docker-compose.yml
├── README.md
├── .gitignore
├── .githooks/
│   ├── commit-msg
│   └── pre-commit
├── .github/
│   ├── workflows/ci.yml
│   └── PULL_REQUEST_TEMPLATE.md
├── mp-common/
├── mp-api/
│   ├── mp-api-activity/
│   ├── mp-api-reward/
│   ├── mp-api-fission/
│   ├── mp-api-benefit/
│   └── mp-api-mock/
├── mp-gateway/
├── mp-activity/
├── mp-reward/
├── mp-fission/            V0 建空壳，V3 才填
├── mp-benefit-order/
└── mp-mock-downstream/
```

**`mp-fission` 在 V0 就建空壳**，理由：module 骨架一次建完，避免 V3 再动根 `pom.xml` 的聚合配置。空 module 的构建成本是零。

### 3.6 V0 退出标准

1. 三个人各自 `git clone` 后，按 `README` 的三步（`mvn validate` 装钩子 → `docker compose up -d mysql` → `mvn verify`）一次通过
2. 提一个 PR，CI 六项全绿
3. 故意提交一条不合规的 commit message，确认被 `commit-msg` 钩子拦下
4. 故意写一行不合格式的代码，确认 `spotless:check` 拦下
5. §3.3 的依赖方向反向验证做过，结论写进 PR
6. 《环境与依赖》§2.1 的版本号回填为精确的三位版本，尤其确认 Dubbo 与 Boot 的组合可用（实测组合：Boot 3.5.16 + Dubbo 3.3.6）
7. 冒烟接口返回的 `traceId` 非空，且日志中同一次请求的多行日志 traceId 一致

> **钩子的先有鸡问题**：`git-build-hook-maven-plugin` 在 `mvn validate` 阶段安装钩子，因此**克隆后第一次提交之前必须先跑一次 `mvn validate`**，否则钩子不生效。这一条写进 `README` 的第一段，不要指望别人自己发现。CI 端的重复校验是这个缺口的兜底。

---

# V1：跑通

> 预计一周。目标是**一条能演示的正向链路**，以及八项形状冻结全部落地。

## 4. V1 范围

### 4.1 一句话

用户下单 → 支付成功 → 权益发放 → 查得到。下游全部 mock 且固定成功，不考虑任何异常。

### 4.2 链路时序

```
① createTrade                            benefit-order
     校验活动有效（调 activity.queryActivityConf）
     读 benefit_sku → benefit_package → benefit_item，组装 benefit_snapshot
     生成 bizNo（BizNoGenerator）—— 同时作为支付侧的商户订单号
     事务：建 play_biz_record（pay=WAIT_PAY, grant=NOT_START, refund=NONE）
          + 写 play_op_record(CREATE_TRADE, op_seq='', SUCCESS)
     事务外：调 mock 支付下单，传 outTradeNo=bizNo
     第二个短事务：回填 trade_no
                ↓
② payCallback                            benefit-order
     按 outTradeNo（=bizNo）定位主单     ← 不依赖 trade_no 是否已回填
     验金额/币种/商户号，任一不符 → 不改任何状态，返回失败
     事务：按 payStatus 分支做条件更新（见下表）
          + 写 play_op_record(PAY_CALLBACK, op_seq=notifySeq)
     仅当推进到 PAY_SUCCESS 时，事务提交后：
          【V1 同步调用 grantBenefit；V2 改为事务内落 GRANT 任务】
                ↓
③ grantBenefit                           benefit-order
     读 play_biz_record.benefit_snapshot，按 provider_type 分组
     事务 A：条件更新 grant_status NOT_START → GRANTING
            + upsert play_op_record(GRANT_BENEFIT, op_seq='', status=PROCESSING)
            ※ 二者 affected_rows 均为 0 时 → 走重入分支（见下）
     事务外，每组派生 grantOpNo = bizNo + '_G_' + providerType
       → 调 reward.grantReward
       → upsert benefit_fulfillment_record（按 uk_biz_item，一权益项一行）
     事务 B：条件更新 grant_status GRANTING → GRANT_SUCCESS
            + 回写 op_record(GRANT_BENEFIT).status = SUCCESS
                ↓
④ reward.grantReward                     reward
     按 op_no 查 reward_grant_record，命中则直接返回原结果（幂等出口）
     未命中：插入 reward_grant_record(result=PROCESSING)   ← 落库态，非下游返回值
            → 调 mock 供应方（V1 固定返回 SUCCESS）
            → 落 reward_grant_item（一奖励项一行）
            → 回写 reward_grant_record.result = SUCCESS
                ↓
⑤ queryOrder                             benefit-order
     返回主单三子状态 + 履约明细列表
```

#### `payStatus` 的分支处置

`PayCallbackReq.payStatus` 在 V1 只接受两个值，各自的条件更新谓词不同：

| `payStatus` | 条件更新 | 后续 |
| --- | --- | --- |
| `SUCCESS` | `SET pay_status='PAY_SUCCESS' WHERE pay_status='WAIT_PAY'` | `affected_rows=1` 才触发 `grantBenefit` |
| `FAILED` | `SET pay_status='PAY_FAILED' WHERE pay_status='WAIT_PAY'` | 不触发履约，链路终止 |

**`affected_rows=0` 时直接 ACK 返回成功，不抛异常、不重试。** 这是幂等三道闸的第三道生效，不是错误。

**V1 不接受 `CLOSED`**：关单是 V2 的 `closeOrder` 引入的语义，V1 收到该值直接拒绝并返回参数错误。不要为了「以后要支持」提前写分支 —— 提前写的分支没有测试覆盖，而 V2 引入 `CLOSING` 中间态后逻辑还要重写。

#### 关于支付侧的关联号

`bizNo`（`play_biz_record_no`）**同时作为传给支付方的商户订单号**（即微信/支付宝语境下的 `out_trade_no`），回调按它定位主单。

不新增字段的理由：`bizNo` 本身已全局唯一、已有 `uk_biz_no`、且在调用支付方**之前**就已落库。若改为「支付方返回 `tradeNo` 后再按 `tradeNo` 定位」，则在「支付下单成功但 `trade_no` 尚未回填」的窗口内进程崩溃，回调将无法定位业务单 —— 钱收了，订单还是 `WAIT_PAY`。

`trade_no` 仍然回填并保留 `uk_trade_no`，它的作用是反查与对账，不是回调的定位依据。

#### 关于 `quantity`

**V1 冻结为 `quantity = 1`**，`createTrade` 校验不等于 1 时直接拒绝（`4001`）。

字段保留在 `CreateTradeReq` 里（形状冻结，V2 引入库存时才真正生效）。V1 不实现的理由：`quantity > 1` 会牵连三处语义——应付金额如何计算、`RewardItem.qty` 传什么、`benefit_fulfillment_record` 是一项一行还是一份一行。这三处都要在库存与限购一起做时才定得下来，V1 定了大概率要改。

**显式校验而非默默忽略**：如果只是不读这个字段，调用方传 `quantity=3` 会得到一份权益却付了一份的钱，且没有任何报错。

#### 关于 `benefit_snapshot`

在 `createTrade` 时从 `benefit_sku` → `benefit_package` → `benefit_item` 组装并落库，履约时只读快照、不再查配置表。这样即使运营改了权益包，历史订单仍按下单时的配置履约。V1 就要这么做 —— 后改要洗数据。

#### `grantBenefit` 重入怎么处理

V1 虽然是 happy path，但 `grantBenefit` 会被重复调用（重复的支付回调、V2 的任务重试）。**这条路径上有三处会命中约束，都不是错误**：

| 位置 | 现象 | 处置 |
| --- | --- | --- |
| 条件更新 `NOT_START → GRANTING` | `affected_rows = 0` | 读出当前 `grant_status`，按下表分支 |
| 插入 `op_record(GRANT_BENEFIT, '')` | `uk_biz_op` 冲突 | **upsert**：`ON DUPLICATE KEY UPDATE retry_count = retry_count + 1` |
| 插入 `benefit_fulfillment_record` | `uk_biz_item` 冲突 | **upsert**：命中则更新 `provider_order_no` / `grant_status`，不新增行 |

**这三处必须写成 upsert 而非 insert**，否则第二次调用会抛 `DuplicateKeyException` 中断链路 —— 而它其实应该正常返回。

按 `grant_status` 的分支：

| 已有 `grant_status` | 处置 |
| --- | --- |
| `NOT_START` | 正常首次执行 |
| `GRANTING` | 继续走发放流程 —— `reward.grantReward` 按 `opNo` 幂等，返回原结果；`benefit_fulfillment_record` 走 upsert 更新为最新结果 |
| `GRANT_SUCCESS` | 直接返回原结果，**不再调用 reward** |

**正确性由 reward 侧的 `uk_op_no` 兜底，不由 benefit 侧的状态判断兜底。** 即使两个线程同时进入 `GRANTING` 分支，`grantOpNo` 是确定性派生的（`bizNo + '_G_' + providerType`），reward 侧只会有一条记录、只发一次奖。状态判断只是避免无谓的 RPC，是优化不是保证。

V2 引入任务调度后，`GRANTING` 分支改为交给查单任务收敛，不再直接重发。

#### `op_record(GRANT_BENEFIT)` 为何跨两个事务

`grantBenefit` 的 `op_record` 先写 `PROCESSING`（事务 A）、调用完成后回写 `SUCCESS`（事务 B），这与 `CREATE_TRADE` / `PAY_CALLBACK` 一次性写 `SUCCESS` 不同。

原因是它包含**跨服务调用**：调用发出前必须先留痕（否则 RPC 发出后崩溃就没有任何记录），调用结果回来后才能判定终态。这正是终态设计中「`op=PROCESSING` + 状态置 `GRANTING` + 查单任务，三者同一事务提交后才发 RPC」的形状（技术方案 §5.3 ④）—— V1 只是暂时缺了查单任务这一项。

**代价**：事务 A 与 B 之间崩溃，`op_record` 永久停在 `PROCESSING`，V1 无机制收敛。这与 §7.3 第 2 条是同一个缺口，V2 的任务表一并补上。

#### 关于 ②→③ 的调用位置

**这是 V1 的临时形态。** V2 改为「payCallback 事务内落 `GRANT` 任务，调度器驱动 `grantBenefit`」，因为任务表在 V2 才引入。冻结项 5（事务边界收成一个方法）保证了这次改动只影响一个方法内部。全部临时形态见 §7.3。

**调用位置必须在事务提交之后**，不在事务内 —— 事务内调 RPC 违反《开发规范》§7.4。V2 改成任务驱动时，正好是在事务内加一行 `insert`、删掉这行同步调用。

**代价要说清楚**：事务提交后、`grantBenefit` 调用前若进程崩溃，订单会停在「已支付未履约」且**没有任何机制能恢复** —— V1 没有任务表。这是 V1 接受的已知缺口，V2 的任务表就是为了补上它。演示时不要刻意制造这个场景。

### 4.3 接口签名

> 以下是 V1 版签名。**`reward` 的签名即终态签名，不裁**（冻结项 6）；`benefit-order` 的签名从终态裁掉本阶段不做的参数，但保留幂等字段。

#### `mp-api-reward`

```java
public interface RewardService {

    /** 统一发放。同 opNo 重复调用返回同结果。 */
    GrantRewardResp grantReward(GrantRewardReq req);

    /** 按 opNo 查单。V1 提供实现但主链路不调用，V2 收敛时使用。 */
    GrantRewardResp queryGrant(String opNo);
}
```

```java
public class GrantRewardReq {
    private String playType;        // FISSION / BENEFIT_SELL
    private String activityId;
    private String bizOrderNo;      // 调用方业务单号 —— 不是「订单号」
    private String opNo;            // 幂等键
    private String receiverId;      // 收奖人 userId
    private List<RewardItem> rewardItems;
}

public class RewardItem {
    private int    itemSeq;             // 组内下标，见下方说明
    private String rewardType;
    private String providerType;
    private String providerProductId;
    private int    qty;
    private boolean core;               // 是否核心权益
}

public class GrantRewardResp {
    private RetStatus retStatus;        // 汇总四分类
    private String    retCode;
    private String    retMsg;
    private List<GrantItemResult> items;
}

public class GrantItemResult {
    private int       itemSeq;
    private RetStatus retStatus;        // 每项独立四分类
    private String    providerOrderNo;
    private String    errorCode;
}
```

**`itemSeq` 的口径**：**相对于本次调用的 `rewardItems` 列表，从 0 起连续编号**，不是订单内全局下标。

因为 `grantOpNo` 的粒度是「一次调用 = 一个供应方」，按 `provider_type` 分组后每组是**独立的一次调用、独立的 `opNo`**，各组的 `itemSeq` 都从 0 开始。`reward_grant_item.uk_op_item(op_no, item_seq)` 的第一维已经隔开了不同组，不会冲突。

以 seed 数据为例（项 A 属 `PROVIDER_A`、项 B 属 `PROVIDER_B`）：

| 调用 | `opNo` | `rewardItems` | `itemSeq` |
| --- | --- | --- | --- |
| 第一次 | `BZxxx_G_PROVIDER_A` | [项 A] | 0 |
| 第二次 | `BZxxx_G_PROVIDER_B` | [项 B] | 0 |

**两次调用的 `itemSeq` 都是 0，这是正确的。** 若误写成全局下标（0 和 1），`benefit_fulfillment_record` 与 `reward_grant_item` 的对应关系就要额外维护映射。

**`playType` 在 V1 的取值**：恒为 `BENEFIT_SELL`（`reward_grant_record.play_type` 是 `NOT NULL`，必须传）。它的作用是让 reward 侧能按玩法维度做路由与对账，V3 接裂变时传 `FISSION`。V1 就要传对，不要留空或传 `null`。

**签名自检**：裂变的师徒双向发奖调用它时，`playType` 传 `FISSION`、`bizOrderNo` 传裂变关系号、`opNo` 传 `outFlowNo + '_FL'`、`receiverId` 传徒弟 userId、`rewardItems` 传奖励包展开项。**能原样调用，不需要改签名。**

#### `mp-api-benefit`

```java
public interface BenefitOrderService {

    CreateTradeResp createTrade(CreateTradeReq req);

    PayCallbackResp payCallback(PayCallbackReq req);

    /** V1 由 payCallback 同步调用；V2 改由 GRANT 任务驱动。 */
    GrantBenefitResp grantBenefit(String bizNo);

    QueryOrderResp queryOrder(String bizNo);
}
```

```java
public class CreateTradeReq {
    private ReqUserInfo userInfo;    // userId, appId
    private String activityId;
    private String skuId;
    private String clientReqNo;      // 幂等键组成部分，客户端生成
    private int    quantity;         // V1 冻结为 1，不等于 1 直接拒绝
    // V2 加：consultToken（咨询凭证）、ReqTerminalInfo、RiskInfo
}

public class PayCallbackReq {
    private String outTradeNo;       // = bizNo，回调据此定位主单
    private String tradeNo;          // 支付侧单号，用于回填与对账
    private String notifySeq;        // 幂等键组成部分，重传时不变
    private String payStatus;        // V1 仅接受 SUCCESS / FAILED —— 不入幂等键
    private long   payAmount;        // 分
    private String currency;
    private String merchantId;
}
```

**`payCallback` 的三处约定，V1 就要写对**：

1. **按 `outTradeNo` 定位，不按 `tradeNo`** —— `tradeNo` 在「支付下单成功但回填前崩溃」的窗口里可能还是 `NULL`
2. **`payStatus` 不入幂等键** —— 入了键，「先到 SUCCESS、后到 CLOSED」的乱序通知两条都能插入，第二条会把已支付订单关闭
3. **`play_op_record.op_seq = notifySeq`，不是空串** —— 见《开发规范》§5.6。取空串会让第二条通知在 `uk_biz_op` 上冲突被拒，执行不到条件更新，V2 的乱序防护会失效

#### `mp-api-activity`

```java
public interface ActivityService {
    /** V1 直接查库，无缓存。V3 加本地缓存 + 版本快照。 */
    ActivityConfResp queryActivityConf(String activityId);
}
```

#### `mp-mock-downstream`

```java
public interface MockPayService {
    /** V1 固定成功，返回 tradeNo。入参含 outTradeNo（= bizNo）。 */
    PayCreateResp createPay(PayCreateReq req);
}

public interface MockProviderService {
    /** V1 固定返回 RetStatus.SUCCESS。返回结构必须是四分类形状。 */
    ProviderGrantResp grant(ProviderGrantReq req);
}
```

**mock 的返回结构必须是四分类形状**，即使 V1 只会返回一个值。V2 给 mock 加注入开关时，只改实现不改契约。

#### 「V1 只实现 SUCCESS 分支」的准确含义

冻结项 2 说其余三个分支 `throw UnsupportedOperationException`，指的是**处理下游返回值的 `switch` 分支**，不是禁止 `PROCESSING` 这个值出现在数据库里。

| 位置 | V1 是否出现非 SUCCESS |
| --- | --- |
| mock 下游的**返回值** | 否，固定 `SUCCESS` |
| 调用方处理返回值的 `switch` | `FAIL`/`PROCESSING`/`UNKNOWN` 三个 case 抛 `UnsupportedOperationException` |
| `reward_grant_record.result` **落库中间态** | **是** —— 调用下游前先落 `PROCESSING`，返回后回写 `SUCCESS` |
| `play_op_record.status` 落库中间态 | **是** —— 同上，`GRANT_BENEFIT` 先 `PROCESSING` 后 `SUCCESS` |

**「调用前先落中间态」是必须在 V1 就做对的形状**：若省略这一步、等结果回来才插记录，则 RPC 发出后崩溃将没有任何痕迹 —— 这正是 V2 查单收敛所依赖的锚点。V1 缺的只是「查单任务」，不是「中间态」。

**mock 支付不自动回调。** `createPay` 只返回 `tradeNo`，不异步触发 `payCallback`。回调由外部触发：

| 场景 | 触发方式 |
| --- | --- |
| 集成测试 | 测试代码主动调用 `payCallback`，自行构造 `notifySeq` |
| 手工演示 | `curl` 打 gateway 的 `/api/benefit/pay-callback` |

**不做自动回调的理由**：自动回调会让「支付完成」变成一个看不见的异步过程，演示时无法暂停在「已付款未履约」这个中间状态观察，V2 要注入回调乱序、重复投递时也无从下手。真实支付平台的通知本来就是外部事件，mock 保持这个形状是对的。

V1 不做验签 —— 签名校验是 L1 防线，V2 随咨询凭证一起做。

#### gateway HTTP 端点

对外响应统一 `{code, message, data, traceId}`，`RetStatus` 的四分类语义不暴露给端（技术方案 §4.1）。

| 方法 | 路径 | 转调 |
| --- | --- | --- |
| POST | `/api/benefit/trade` | `createTrade` |
| POST | `/api/benefit/pay-callback` | `payCallback` |
| GET | `/api/benefit/order/{bizNo}` | `queryOrder` |

`grantBenefit` 不对外暴露 —— 它是内部编排入口，V1 由 `payCallback` 调用，V2 由任务调度器驱动。

`RetStatus` → HTTP `code` 的转换规则：

| RetStatus | code | 说明 |
| --- | --- | --- |
| `SUCCESS` | `0` | |
| `FAIL` | 原 `retCode`（`1xxx`/`4xxx`） | 展示给用户 |
| `PROCESSING` / `UNKNOWN` | `0`，`data.status` 标记处理中 | **对端不返回失败** —— 结果未定时告诉用户「失败」，用户会重试，形成重复下单 |

上表是终态规则。V1 下游固定成功，仅 `SUCCESS` 一行被真实走到，`data.status` 字段与其余三行的分支留待 V2 填实现。

**业务拒绝与请求级失败分开处置**：业务拒绝走 HTTP 200 + 业务码，由端侧按 `code` 分支；路由不存在、方法不支持、报文不可读属请求级失败，保留框架给出的 4xx，仅套用统一响应壳。二者不可合并 —— `5xxx` 的语义是「结果未知，按 UNKNOWN 查单收敛」，若把客户端错误记为 `5001`，调用方会为一件未发生的事发起重试与对账。

### 4.4 建哪些表

**按技术方案的终态 DDL 全量建，不裁字段**（理由见《开发规范》§4.4）。

| 库 | 表 | DDL 出处 | Flyway 文件 |
| --- | --- | --- | --- |
| `db_activity` | `marketing_activity` | §3.2 | `V1001__create_marketing_activity.sql` |
| `db_activity` | — | — | `V1090__seed_activity.sql`（演示数据） |
| `db_benefit` | `benefit_sku` | §3.4 | `V1101__create_benefit_sku.sql` |
| `db_benefit` | `benefit_package` / `benefit_item` | §3.4 | `V1102__create_benefit_package.sql` |
| `db_benefit` | `play_biz_record` | §3.4 | `V1103__create_play_biz_record.sql` |
| `db_benefit` | `benefit_fulfillment_record` | §3.4 | `V1104__create_benefit_fulfillment_record.sql` |
| `db_benefit` | `play_op_record` | §3.4 | `V1105__create_play_op_record.sql` |
| `db_benefit` | — | — | `V1190__seed_sku.sql`（演示数据） |
| `db_reward` | `reward_grant_record` | §3.5 | `V1201__create_reward_grant_record.sql` |
| `db_reward` | `reward_grant_item` | §3.5 | `V1202__create_reward_grant_item.sql` |

**唯一索引一个都不能省**：`play_biz_record.uk_idempotent`、`play_op_record.uk_idempotent` + `uk_biz_op`、`benefit_fulfillment_record.uk_biz_item`、`reward_grant_record.uk_op_no`。V1 虽然没有并发，但索引是 schema 层的，后加要洗数据。

> **上表的库归属是终态形态。V1 实际以单数据源承载全部表**，多数据源在 V2 配置，见 §7.3 第 8 条。迁移脚本按库归属放置于对应 module，拆库时无需移动文件。

**V1 不建的表**：`activity_config_version`、`activity_op_record`、`activity_task`、`marketing_stock`、`user_purchase_quota`、`reward_revoke_record`、全部 `*_task`、全部 `fission_*`。这些在 V1 没有写入方，建了也是空表。

#### seed 数据

`V1090` / `V1190` 两个脚本负责初始化演示数据，规模要够小、够固定，让集成测试能写死断言：

| 对象 | 内容 |
| --- | --- |
| 活动 | 1 个，`activity_id='ACT_DEMO_001'`，`play_type='BENEFIT_SELL'`，`status='ONLINE'`，起止时间覆盖 2025-2030 |
| 权益包 | 1 个，`package_version=1`，含 **2 个权益项** |
| 权益项 | 项 A：`provider_type='PROVIDER_A'`、`is_core=1`；项 B：`provider_type='PROVIDER_B'`、`is_core=0` |
| SKU | 1 个，`sku_id='SKU_DEMO_001'`，`sale_price=9900`（99 元），绑定上述权益包 |

**两个权益项分属不同 `provider_type` 是刻意设计的** —— 这样 `grantBenefit` 的「按供应方分组、每组一个 `grantOpNo`」逻辑在 V1 就被真实走到，而不是只有一组时退化成直接调用。若两项同属一个供应方，分组代码写错了 V1 也测不出来。

#### `config_version` 在 V1 取什么值

`play_biz_record.config_version` 是 `NOT NULL`，但 V1 不建 `activity_config_version` 表 —— 需要显式约定，否则实现时会随手填 0 或 -1。

| 字段 | V1 取值 |
| --- | --- |
| `marketing_activity.cur_version` | seed 里写 `1` |
| `play_biz_record.config_version` | 建单时读 `marketing_activity.cur_version`，即恒为 `1` |

**V3 建版本表时补一行 `(activity_id='ACT_DEMO_001', version=1)` 的快照**，历史订单的 `config_version=1` 正好对上，无需数据迁移。

代码按「读活动的 `cur_version` 并冻结进主单」来写，而不是硬编码 1 —— 逻辑与终态一致，只是 V1 阶段这个值恒为 1。

### 4.5 各 module 装什么

| module | V1 内容 |
| --- | --- |
| `mp-common` | `RetStatus` 枚举、`BizNoGenerator`、`IdempotentKeys`、错误码常量、`BizException`、`TraceIdFilter` 支撑类 |
| `mp-api-*` | §4.3 的接口与 DTO，无实现 |
| `mp-gateway` | 3 个 HTTP 端点、`@RestControllerAdvice` 全局异常处理、traceId 过滤器、统一响应壳封装 |
| `mp-activity` | `queryActivityConf` 实现，直接查 `marketing_activity` |
| `mp-reward` | `grantReward` / `queryGrant` 实现；`op_no` 幂等；调 mock 供应方；落主表 + 明细表 |
| `mp-benefit-order` | 4 个接口实现；三子状态条件更新；`play_op_record` 写入；按 `provider_type` 分组编排发奖 |
| `mp-mock-downstream` | `MockPayService` / `MockProviderService`，固定成功 |
| `mp-fission` | 空壳，V3 填 |

**`mp-common` 里两个工具类是 V1 必须有的**，把《开发规范》§5 的规则集中在一处，review 只需盯这两个类：

```java
/** 业务单号：全局唯一，每次调用必须不同。UUIDv7 —— 近似有序且随机空间充足 */
public final class BizNoGenerator {
    public static String bizNo()         { return gen("BZ"); }   // play_biz_record_no
    public static String fulfillmentNo() { return gen("FF"); }   // 履约明细号
    private static String gen(String prefix) {
        return prefix + UuidCreator.getTimeOrderedEpoch().toString().replace("-", "");
    }
}

/** 幂等键：确定性可重算，同一操作重试必须得到同一个值 */
public final class IdempotentKeys {
    /** 履约发奖：一次调用 = 一个供应方 */
    public static String grantOpNo(String bizNo, String providerType) {
        return bizNo + "_G_" + providerType;
    }
    /** 支付回调 —— payStatus 不入键 */
    public static String payCallback(String tradeNo, String notifySeq) {
        return tradeNo + "_" + notifySeq;
    }
    // V2 补：closeOrder；V3 补：refundNo、revokeNo、followerGrantNo、sponsorFlowNo
}
```

**两个类的规则相反**，放在一起是为了让这个反差在代码里可见：`BizNoGenerator` 必须用随机源，`IdempotentKeys` 恰恰禁止随机源。把它们写成一个工具类是常见的错误起点。

**不用「时间戳 + 8 位随机」**：32 bit 随机空间在同一秒内约 7.7 万次调用即有 50% 碰撞概率，压测（500 VU）与并发重试下不是理论风险。UUIDv7 前 48 bit 是毫秒时间戳，同样近似有序（降低 B+ 树页分裂），但随机空间足够。理由详见《开发规范》§5.2。

**无论生成器多可靠，插入路径都要处理唯一冲突**：捕获 `DuplicateKeyException` → 重新生成 → 重试，最多 3 次。生成器是概率保证，唯一索引是确定性保证。

### 4.6 明确不做什么

显式声明，避免评审时误以为遗漏。

| 不做项 | 排在 | 理由 |
| --- | --- | --- |
| 库存与限购 | V2 | 属并发正确性范畴，与幂等三道闸一起做才有对照 |
| 可靠任务表与调度器 | V2 | 事务边界冻结后，加任务表只是加一行 `insert` |
| 四分类的另外三个分支 | V2 | 枚举 V1 已定，V2 只填实现 |
| L1 咨询凭证签名 | V2 | 与 L2/L3/L4 一起构成四层防线，整体做 |
| L2 分布式锁 | V2 | 套在方法外层，不改内部逻辑 |
| 订单关闭与 `CLOSING` | V2 | 依赖任务表 |
| 退款、回收 | V3 | 逆向链路依赖发放链路稳定 |
| 裂变全链路 | V3 | 见 §1.2 |
| 对账 | V3 | 纯旁路读，不侵入主链路 |
| 好友过滤下推 | V3 | 裂变的一部分，纯 SQL 优化 |
| RocketMQ 事件广播 | V3 | 新增订阅方，零侵入 |
| Nacos / Dubbo `tri` / 多实例 | V3 | 配置与注解层面 |
| Sentinel / SkyWalking | V3 | 旁路接入 |
| 活动/SKU 管理接口 | 范围外 | 配置数据由 seed SQL 初始化 |

### 4.7 V1 退出标准

**功能（集成测试断言）**

1. e2e 跑通：`createTrade` → `payCallback` → `grantBenefit` → `queryOrder`
2. 主单：`pay_status=PAY_SUCCESS`、`grant_status=GRANT_SUCCESS`、`refund_status=NONE`
3. 明细：`benefit_fulfillment_record` **2 行**（seed 的两个权益项），每行 `grant_status=SUCCESS` 且 `provider_order_no` 非空
4. 留痕：`play_op_record` **3 条** —— `CREATE_TRADE`(`op_seq=''`)、`PAY_CALLBACK`(`op_seq=notifySeq`)、`GRANT_BENEFIT`(`op_seq=''`)，均为 `status=SUCCESS`
5. 发奖侧：`reward_grant_record` **2 行**（两个 `provider_type` 各一），`reward_grant_item` **2 行**
6. 快照：`play_biz_record.benefit_snapshot` 非空，内容与下单时的权益包一致
7. 支付关联：回调按 `outTradeNo` 定位成功；`trade_no` 已回填且与 mock 返回值一致

**幂等与分支（集成测试断言）**

8. 重复投递同一 `clientReqNo` 的下单请求，第二次**返回原单**而非建新单，`play_biz_record` 仍为一行（冻结项 1）
9. 重复调用同一 `opNo` 的 `grantReward`，第二次**返回原结果**，`reward_grant_record` 仍为一行（冻结项 1）
10. 重复投递同一 `notifySeq` 的支付回调，第二次**不改变任何状态**，`play_op_record` 的 `PAY_CALLBACK` 仍为一条（冻结项 1）
11. 投递**不同 `notifySeq`** 的第二条回调，`play_op_record` 新增一条，但主单状态不变（`affected_rows=0`）—— 验证 `op_seq=notifySeq` 与条件更新的分工
12. `payStatus=FAILED` 的回调使主单进入 `PAY_FAILED`，**不触发履约**，`benefit_fulfillment_record` 为空
13. `payStatus=CLOSED` 的回调被拒绝，返回参数错误，不改变任何状态
14. `quantity != 1` 的下单请求被拒绝，返回 `4001`，不建单
15. 重复调用 `grantBenefit`（主单已 `GRANT_SUCCESS`），第二次**直接返回原结果不调 reward**：`benefit_fulfillment_record` 仍为 2 行，`reward_grant_record` 仍为 2 行，`op_record(GRANT_BENEFIT)` 仍为 1 条
16. 在 `grant_status=GRANTING` 时再次调用 `grantBenefit`，**不抛 `DuplicateKeyException`** —— 验证三处 upsert（`op_record` / `benefit_fulfillment_record` / 条件更新）都按重入路径正确处理
17. 连续创建两笔订单（`trade_no` 均未回填），第二笔建单成功 —— 验证 `uk_trade_no` 上多行 `NULL` 可共存
18. `play_biz_record.config_version` = `marketing_activity.cur_version` = 1
19. `reward_grant_item.item_seq` 在两次调用中**都为 0**（各组独立编号，非全局下标）
20. `reward_grant_record.play_type` = `BENEFIT_SELL`，非空

**形状冻结验证（review 与静态检查）**

21. `RetStatus` 枚举四个值齐全，处理**下游返回值**的 `switch` 中 `FAIL` / `PROCESSING` / `UNKNOWN` 三个 case 为 `throw UnsupportedOperationException`（冻结项 2）
22. **调用下游前已落中间态**：`op_record(GRANT_BENEFIT)` 与 `reward_grant_record` 在 RPC 发出前即为 `PROCESSING`，不是等结果回来才插记录
23. 五个结果类枚举（`RetStatus` / `OpStatus` / `GrantStatus` / `ItemGrantStatus` / `PayStatus`）各自独立定义，落库时未互相套用 —— 重点核对两处：`downstream_result` 写 `FAIL` 而 `status` 写 `FAILED`；主单 `grant_status` 写 `GRANT_SUCCESS` 而明细 `grant_status` 写 `SUCCESS`（《开发规范》§6.5）
24. 三子状态枚举与技术方案 §3.4 的状态迁移表逐一对齐，包括 V1 用不到的值（冻结项 3）
25. 状态变更全部走条件更新，全局搜索确认无「先 SELECT 判状态再 UPDATE」（冻结项 5）
26. 全局搜索 `@Transactional`，确认每处方法内均无 RPC 调用（冻结项 5）
27. `GrantRewardReq` 中不含任何权益售卖专有概念 —— 由 review 逐字段确认（冻结项 6）
28. 各 module 独立编译通过，`mvn dependency:tree` 确认无反向依赖（冻结项 7）
29. 全局搜索 `catch`，确认无 `catch (Exception) → FAIL` 的路径（冻结项 8）
30. `BizNoGenerator` 有单测：连续生成 10 万个单号无重复；插入路径有唯一冲突重试

**工程**

31. `mvn clean verify` 通过
32. V0 的 `smoke_record` 表与全部 `smoke` 方法已删除（新写一个 `V1203__drop_smoke_record.sql`，不修改 V0 的历史脚本）
33. 本地库删库重建（`docker compose down -v`）后，Flyway 从零跑通全部迁移

### 4.8 V1 实测记录

**交付**：PR #1，squash 合入 `main`（`fcec207`），74 个文件、4966 行新增。CI 六项全绿。

**测试规模**：42 个用例。9 个单元测试（`BizNoGeneratorTest` 4、`IdempotentKeysTest` 5）、10 个形状冻结检查（`ShapeFreezeTest`）、23 个集成测试（`ForwardChainIT` 6、`IdempotencyIT` 8、`BranchRejectionIT` 5、`HttpEndpointIT` 4）。集成测试跑真实 MySQL 容器，Flyway 每次从零执行 12 个迁移脚本。

#### 退出标准达成情况

33 条中 32 条由自动化断言覆盖，1 条部分覆盖。

| 分组 | 条目 | 覆盖方式 |
| --- | --- | --- |
| 功能 1–7 | 7 条 | `ForwardChainIT` |
| 幂等与分支 8–20 | 13 条 | `IdempotencyIT`、`BranchRejectionIT`、`ForwardChainIT` |
| 形状冻结 21–29 | 9 条 | `ShapeFreezeTest`（`*Test` 命名，不依赖数据库） |
| 形状冻结 30 | 1 条 | **部分覆盖**，见下 |
| 工程 31–33 | 3 条 | CI + 人工执行 |

**第 30 条未完全覆盖**：`BizNoGenerator` 的 10 万次无重复单测已有；但「插入路径唯一冲突重试」这一分支无集成测试 —— 单号由服务内部生成，不注入生成器则无法在测试中制造碰撞。该分支目前仅由 review 保证。测试 `IdempotencyIT.idempotentHitNeverReturnsAnotherUsersOrder` 的 javadoc 中已标明此覆盖边界，避免后续误读为已覆盖。

**第 21、25、26、29 条改为静态检查而非 review**。原计划由人工 review 确认，实施时机械化为测试：`ShapeFreezeTest` 遍历 `src/main/java` 全部文件，断言无 `catch (Exception)` 块在 400 字符内出现 `RetStatus.FAIL` 或 `OpStatus.FAILED`、条件更新 SQL 均含 `#{fromStatus}`、`@Transactional` 方法体内无 RPC 调用。理由：这些约束每次改动都需重新成立，而人工核对只在核对当天有效。

#### 八项形状冻结落地情况

| # | 冻结项 | 落地情况 |
| --- | --- | --- |
| 1 | 幂等键进接口 + 唯一索引 | 达成。`uk_idempotent` / `uk_biz_op` / `uk_biz_item` / `uk_op_no` / `uk_op_item` 全部建立，幂等由唯一索引兜底而非「先查后插」 |
| 2 | 下游返回四分类枚举 | 达成。`RetStatus` 四值齐全，非 `SUCCESS` 分支为 `throw UnsupportedOperationException` |
| 3 | 状态枚举一次定全 | 达成。`PayStatus` 5 值（含 V2 才用的 `CLOSING`）、`GrantStatus` 5 值、`RefundStatus` 6 值，与技术方案 §3.4 逐值对齐 |
| 4 | `play_op_record` 建表并写入 | 达成。三个操作各留一条，`op_seq` 取值分别为 `''` / `notifySeq` / `''` |
| 5 | 事务边界收成一个方法 | 达成，但实现形态与预期不同，见下方偏差 1 |
| 6 | `reward` 接口通用化 | 达成。`ShapeFreezeTest` 断言 `GrantRewardReq` 字段恰为六项，且不含 `trade` / `sku` / `pay` / `benefit` / `refund` 子串 |
| 7 | Maven 模块按终态切 | 达成。`mvn dependency:tree` 确认无反向依赖。`mp-api` 实际拆为 5 个子模块（含 `mp-api-mock`），比原计划多一个 |
| 8 | 异常映射方向 | 达成，但实施中发现原设计有缺口，见下方缺陷 2 |

#### 实施中发现的缺陷

三处，均为实施期自查发现，非 review 发现（本阶段未走 review 流程，见下）。

**① `@Transactional` 在同类内部调用中静默失效。** 初版将事务方法与编排逻辑写在 `BenefitOrderServiceImpl` 同一个类中。Spring 的 `@Transactional` 依赖代理，同类内部调用不经过代理，注解不生效 —— 且不报错。后果是「状态已改但操作记录未落库」，而这正是可靠任务表最依赖的前提。修正：抽出独立 bean `OrderTxService`，五个事务方法各为一个事务单元，方法体内无 RPC、无消息、无 sleep。

**② 全局异常处理器把请求级失败吞成 `5001`。** 由 `HttpEndpointIT.grantBenefitIsNotExposedOverHttp` 发现：请求一个不存在的路由，期望 404，实测 500 + `5001`。原因是 `@ExceptionHandler(Exception.class)` 捕获了 `NoResourceFoundException`。`5xxx` 的语义是「结果未知，按 UNKNOWN 查单收敛」，一个 URL 拼错的请求若返回 `5001`，调用方会为一件未发生的事发起重试与对账。修正：`GlobalExceptionHandler` 继承 `ResponseEntityExceptionHandler`，沿用框架对十余种 MVC 异常的状态码映射，仅覆写响应体包装。

此项是对冻结项 8 的补充：原表述为「未预期异常 → `UNKNOWN`」，隐含前提是「到达业务逻辑的异常」。请求级失败（路由不存在、方法不支持、报文不可读）根本未进入业务逻辑，映射为 `UNKNOWN` 是错的。冻结项 8 的完整表述应为：业务拒绝 → `FAIL`，**业务逻辑内**的未预期异常 → `UNKNOWN`，**未进入业务逻辑**的请求级失败 → 保留框架 4xx。

**③ 建单时两种唯一冲突被合并处理。** `play_biz_record` 上有三道唯一索引，初版 `catch (DuplicateKeyException)` 一律按幂等命中处理并返回查到的单。但 `uk_biz_no`（单号碰撞）与 `uk_idempotent`（幂等命中）的正确处置相反：前者应换号重试，后者应返回原单。合并处理的后果是**单号碰撞时把另一个用户的订单作为本次结果返回**。修正：`insertOrder` 以幂等键回查区分二者，碰撞时换号重试，最多 3 次。

**④ 附带修复：pre-commit 钩子在非交互 shell 下找不到 `JAVA_HOME`。** git 钩子不读 `.zshrc`，表现为终端里 `mvn` 正常、一提交就报「JAVA_HOME 未正确定义」。三名成员都会遇到。修正：钩子自行解析 JDK 路径，按《环境与依赖》§1.1 的 brew formula 路径兜底（Apple Silicon / Intel 两处），均不存在时给出指向该章节的错误提示。

#### 与计划的偏差

| # | 计划 | 实际 | 原因 |
| --- | --- | --- | --- |
| 1 | 事务边界「收成一个方法」 | 收成一个独立 bean `OrderTxService`，含五个事务方法 | 单个方法无法覆盖建单、回填单号、回调、履约起止五个边界；关键在于事务方法与编排逻辑分属不同 bean，而非数量为一 |
| 2 | `mp-api` 拆 4 个子模块 | 5 个，增 `mp-api-mock` | mock 契约需被 `benefit-order` 与 `reward` 共同依赖，放在 `mp-mock-downstream` 内会造成玩法层依赖实现模块 |
| 3 | 四个库各自 Flyway | 单数据源承载全部表 | 见 §7.3 第 8 条 |
| 4 | seed 脚本 `V1091` / `V1191` | `V1090` / `V1190` | 文档笔误，实现取的是同章表格中的正确编号 |

**V0 阶段文档回填的四处**：Spring Boot 版本（3.2.x → 3.5.16）、failsafe 需显式指定 `classesDirectory`（不继承 `spring-boot-starter-parent` 时的必要配置，缺失时报错信息完全指不到原因）、编译需开 `-parameters`、Dubbo `injvm` 在单进程下的注解冗余。已回填至《环境与依赖》§2.1、§2.2。

#### 流程偏离

**本阶段未执行 review。** 《开发规范》§2 要求「每个 PR 至少一人 review 通过方可合入」，PR #1 未经 review 直接 squash 合入。三处缺陷均由实施期自查与集成测试发现，非 review 产出。

该条规范对三人协作项目的价值在于互相理解代码 —— 跳过一次不影响交付，成为默认则规范失效。V2 起恢复执行。

**V0 退出标准第 1 条仍未闭环**：另两名成员尚未在各自机器上执行 `git clone` + `mvn clean verify`。上述缺陷 ④ 的修复效果需由此验证。

---

# V2：扛住

> 骨架，V1 结束后细化再评审。

## 5. V2 范围（待细化）

**主题**：让系统面对「下游不可靠」和「并发」。

| 能力 | 技术方案章节 |
| --- | --- |
| 四分类收敛：mock 可注入 timeout / PROCESSING / FAILED；`UNKNOWN` 保持中间态并以原 `opNo` 查单 | §6.6 |
| 可靠任务表：`benefit_task` 建表；调度器 `SKIP LOCKED` 抢占 + 租约 | §6.5、§7.3 |
| 幂等三道闸：① 幂等键唯一 ② 单据级唯一 `uk_biz_op` ③ 主单条件更新 | §6.4 |
| 库存 0 超卖：`marketing_stock` DB 原子扣减；`user_purchase_quota` 限购 | §7.4 |
| L1 凭证签名：`preConsult` 签发，`createTrade` 验签 + 服务端重算比价 | §6.2 |
| L2 分布式锁：Redisson | §6.3 |
| 订单关闭：`closeOrder` + `CLOSE_ORDER` 任务 + `CLOSING` 中间态 | §4.5 |
| 多数据源：四库各自 `DataSource` + Flyway，`@MapperScan` 按包绑定 | §3.1、《开发规范》§4.5 |
| `mp-reward` 事务边界：补 `RewardTxService` | 《开发规范》§7.4 |

**实现顺序**：先多数据源，再四分类 + 任务表，最后幂等三道闸 + 并发。

多数据源排在最前，因为它是**改结构**而非加功能（§7.3 第 8 条）：任务表、库存表都要落到各自的库，先拆库再建表，可避免建完再迁。四分类与并发的顺序理由同前：先让系统面对不确定的下游，再面对并发，问题才好归因。

**退出标准（初稿）**

1. mock 注入超时，主单停在 `GRANT_UNKNOWN`，查单按短退避（1s→5s→30s）收敛为 `GRANT_SUCCESS`，期间**无重复发放**
2. mock 返回 `PROCESSING`，查单按长退避（30s→2m→10m）收敛，退避序列与场景 1 不同
3. 支付回调乱序（先 `SUCCESS` 后 `CLOSED`），条件更新使迟到的 `CLOSED` `affected_rows=0`
4. 关单 RPC 返回 `UNKNOWN` 时进入 `CLOSING`，**且此时未释放库存与额度**
5. k6 500VU 抢 100 库存：售出恰好 100，无超卖、无重复发放
6. 四库各自持有独立的 `flyway_schema_history`，表分布与 §4.4 的库归属一致；在 `mp-benefit-order` 中写一条 JOIN `db_reward` 表的查询，确认其运行期失败

**故障注入必须人工跑一遍**：自动化测试验证的是「代码逻辑对不对」，被追问的是「你观察到了什么现象」。每个场景由人在终端跑一遍，记录注入方式、状态迁移序列、收敛耗时、期间是否有重复发放，写进本章。

---

# V3：铺开

> 骨架，V2 结束后细化再评审。

## 6. V3 范围（待细化）

| 能力 | 技术方案章节 |
| --- | --- |
| 裂变全链路：13 个接口、关系状态机、`active_flag` 部分唯一、`granting_until` 过期豁免、双向发奖 | §3.3、§4.4、§5.1 |
| 好友过滤下推：`page × N` → `page × 1`，索引 `(group_id, follower_id, status)` | §5.5、§7.1 |
| 退款与回收：`revokeAndAdmit` + `createRefund`，先回收后退款 | §4.5、§5.6 |
| 对账：14 项，资损哨兵指标由对账产出 | §6.8 |
| 事件广播：`providerCallback` → `RewardGrantResultEvent` → 玩法层订阅 | §1.5、§6.7 |
| 分布式化：`injvm` → `tri`，Nacos 注册发现，5 服务独立部署 | §1.4、§7.6 |
| 多实例与可观测：三机部署、Sentinel、SkyWalking、Prometheus + Grafana | §1.6、§8 |

**退出标准（初稿）**

1. **裂变接入后，`reward` 接口签名零改动**（若有改动，如实记录改了什么、为什么 —— 这本身是有价值的交付物）
2. 好友过滤 before/after 压测对照（SQL 次数、扫描行数、P99）
3. 对账能主动检出**人为注入的差异**（如手工改库制造「已收款未发奖」）
4. kill 掉一个实例，其持有的租约到期后任务被另一实例接管并完成
5. RocketMQ 停机时收敛时间退化到查单周期，但仍然收敛、无资损
6. 三机部署下全链路压测通过，Grafana 看板可用

---

## 7. 延后成本评估

### 7.1 零返工延后项

这些是**加东西**不是**改东西**，延后不产生利息：

| 能力 | 延后为何零成本 |
| --- | --- |
| Redisson 分布式锁 | 套在方法外层，不改内部逻辑 |
| 可靠任务调度器 | 事务边界（冻结项 5）收好了，加任务就是加一行 `insert` |
| RocketMQ 事件广播 | 新增订阅方，对发布侧和现有链路零侵入 |
| 对账 | 纯旁路读，不侵入主链路 |
| 好友过滤优化 | 纯 SQL + 索引改动，不动接口 |
| Sentinel / SkyWalking | 旁路接入，配置级 |
| 裂变模块本身 | 独立 module，不修改既有模块（**前提是冻结项 6 做对**） |

### 7.2 有返工风险的一处

**V3 接裂变时，公共能力层的抽象大概率需要调整，预估改动量 20%。**

冻结项 6 能挡掉大部分，但挡不干净 —— 一个抽象对不对，只有第二个使用者接入时才能验证。这是设计的固有性质，不是排期能规避的。

**处置**：接入过程中如实记录公共能力层动了哪几处、每处为什么。讲「接第二个玩法时公共能力层动了哪三处、为什么」，比「我一次设计对了」更可信。

### 7.3 V1 明知不对但先这么做的十处

| # | 做法 | 为什么先这样 | 后续怎么改 |
| --- | --- | --- | --- |
| 1 | `payCallback` 事务提交后同步调用 `grantBenefit` | 任务表在 V2 才引入 | V2 改为事务内落 `GRANT` 任务，调度器驱动。影响范围限于 `payCallback` 一个方法内部 |
| 2 | **「已支付未履约」无兜底机制** | 无任务表即无收敛通路 | V2 的可靠任务表就是为补这个缺口而做 |
| 3 | 四分类只实现 `SUCCESS` 分支 | mock 固定成功，其余分支无法验证 | V2 填三个分支的实现。枚举与调用点的分支结构 V1 已定，只加实现不改结构 |
| 4 | `createTrade` 不预占库存 | 库存在 V2 引入 | V2 在同一事务内加原子扣减。事务边界 V1 已收好，是加语句不是改结构 |
| 5 | `quantity` 冻结为 1 | 三处语义要与库存一起定 | V2 随库存与限购一并放开 |
| 6 | `grantBenefit` 串行调用各供应方 | V1 只有两组，串行不影响演示 | V2 改虚拟线程扇出（技术方案 §7.2），且必须用 `invokeAll` 而非 fail-fast 语义 |
| 7 | 支付回调不验签 | L1 防线随咨询凭证一起做 | V2 引入 |
| 8 | **单数据源承载四个库的表** | 单进程形态下配多数据源无收益，且会拖慢 V1 交付 | V2 配四套 `DataSource` + `SqlSessionFactory` + Flyway `locations`，按 mapper 包路径绑定。见下方展开 |
| 9 | `RewardServiceImpl.queryGrant` 查无记录时返回 `FAIL` | V1 仅在幂等冲突后调用，此时记录必然存在 | V2 改为 `UNKNOWN`。查单任务驱动后，「查不到」可能是尚未提交或主从延迟，判 `FAIL` 会让调用方走失败分支 |
| 10 | `mp-reward` 无事务边界类 | 单项发放、下游固定成功，多次自动提交观察不到中间态 | V2 补 `RewardTxService`，与 `OrderTxService` 同构：中间态落库、明细写入、终态回写各为一个事务单元 |

**第 2 条是 V1 最大的缺口**，也是最值得在评审时主动交代的一条：V1 的正向链路能跑通，但一旦在「支付成功后、履约调用前」这个窗口崩溃，订单就永久停在已收款未发奖，没有任何机制能发现或恢复。这恰好说明为什么可靠任务表不是「锦上添花的中间件」，而是这类系统的骨架 —— **V2 不是在给 V1 加功能，是在补 V1 结构上的洞**。

#### 第 8 条展开：单数据源的代价

V1 实际形态：`application-local.yml` 仅配一个 `DataSource` 指向 `db_reward`，`flyway.locations: classpath:db/migration` 将全部 12 个迁移脚本执行于该库。`docker-compose` 创建的另外三个库为空。

**与《开发规范》§4.5 的关系**：该章约定「禁止跨库 JOIN，禁止跨库本地事务」，其价值在于把 V3 的服务边界前置到开发期 —— 违规在编译或运行期即暴露，而非拆分时才发现。单库形态下这道约束失去强制力：当前代码无跨库 JOIN 是因为尚无此类查询，而非架构拦截。V2 引入对账与库存后，跨表统计需求会出现，届时约束必须已经生效。

**返工性质**：本条不属 §7.1 的「零返工延后项」。多数据源涉及 `DataSource` / `SqlSessionFactory` / `TransactionManager` 三组 Bean 的拆分、`@MapperScan` 按包绑定、Flyway 分库配置，以及集成测试基类的容器与数据源初始化 —— 是改结构，不是加语句。延后至 V2 的成本约一个工作日，延后至 V3 则需同时处理服务拆分，成本不可控。

**V2 的验收方式**：在 `mp-benefit-order` 中写一条 JOIN `db_reward` 表的查询，确认其在运行期失败。约束是否生效以此为准，不以「代码里没有跨库 JOIN」为准。

**显式记下来**，避免 V2 接手的人以为这是设计缺陷，也避免评审时被问「你这不是有问题吗」答不上来 —— 这十处都是**已知的、有计划的**临时形态，不是疏漏。

### 7.4 分布式能力的验证前置说明

租约抢占、`SKIP LOCKED`、实例故障接管这些能力，**在单实例上无法被验证** —— V1/V2 阶段代码写了也观察不到现象。

因此统一放在 V3 验收，验收方式必须是**多实例 + 主动 kill**，而不是「代码里有这个逻辑」。V3 的 `docker-compose` 需支持三实例启动。

---

## 8. 协作方式

| 环节 | 做法 |
| --- | --- |
| 方案 | 每阶段开工前在本文档对应章节补写细化（表、接口、时序），发群 review |
| 评审 | 三人 review 通过后开工，**不另起方案文件** |
| 实现 | 每阶段由一人主导实现，其余两人 review 代码 |
| 轮换 | 按阶段轮换实现人，避免出现「只 review 没写过」的情况 |
| 收口 | 阶段结束前跑一遍退出标准，逐条打钩，未过不进下一阶段 |

每阶段固定产出：代码（CI 全绿）、集成测试（覆盖每条退出标准）、本文档对应章节的实测记录与偏差说明。

---

## 9. 与技术方案的追溯

| 阶段 | 覆盖技术方案章节 |
| --- | --- |
| V0 | §1.4 模块化单体骨架 |
| V1 | §1.2 分层、§3.2/3.4/3.5 部分表、§4.1 幂等键规约、§4.2/4.3/4.5 部分接口、§5.2/5.3 主干 |
| V2 | §6.2~6.6 四层防线与四分类、§7.3 任务调度、§7.4 库存、§5.4 UNKNOWN 收敛 |
| V3 | §3.3 裂变库、§4.4 裂变接口、§5.1/5.5/5.6、§6.7 消息、§6.8 对账、§7.1 好友过滤、§7.6 拆分、§8 监控压测 |

技术方案全部章节在 V3 结束时被覆盖。未被任何阶段覆盖的章节视为遗漏，需补入。

---

（分阶段方案 v1.0 完）
